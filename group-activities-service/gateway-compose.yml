services:
  api-gateway:
    image: nginx:alpine
    container_name: lms_api_gateway
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - lms_network

networks:
  lms_network:
    external: true
    name: lms_network